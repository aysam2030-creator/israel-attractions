1. **Optimize Leaflet Icons Creation in `App.tsx`**:
   - The current code calls `makePin()` inside `mapAttractions.map()` for every attraction on every render.
   - I will extract a memoized cache `Map` for `L.DivIcon`s outside the component (or inside using `useMemo`), so that it caches the icons generated by `makePin`.
   - Update the `icon` prop of `<Marker>` to use the cached icon, which prevents Leaflet from completely unmounting and remounting or re-rendering all markers every time the parent re-renders.
2. **Optimize `Marker` Event Handlers in `App.tsx`**:
   - The code currently passes `eventHandlers={{ click: () => setSelected(a) }}` inside the `.map()` loop, creating a new object on every render.
   - I will extract the Leaflet marker rendering logic into its own separate memoized component, e.g., `AttractionMarker`. This will wrap the `Marker` and `Popup` elements and cleanly isolate its own event handlers and props to keep them stable and prevent marker thrashing.
3. **Verify Changes**:
   - Run `npm run lint` and `npx tsc -b`. Check that there are no TS errors or breaking changes.
4. **Complete Pre-Commit Steps**:
   - Complete pre commit steps to make sure proper testing, verifications, reviews and reflections are done.
5. **Submit PR**:
   - Push code with a descriptive PR "⚡ Bolt: [performance improvement]".
